using UnityEngine;
using System.Collections;

public class MainMenuBehavior : MonoBehaviour {
	/*
	public GameObject mAboutObject;
	public GameObject mMainMenuObject;
	public GameObject mNewGameObject;
	public GameObject mLoadMenuObject;
	public Sprite mButton;
	public Sprite mButtonOver;
	// Use this for initialization
	void Start () {
		mMainMenuObject.SetActive(true);
		mAboutObject.SetActive(false);

	}
	
	// Update is called once per frame
	void Update () {
	
	}

	void OnMouseDown(){
		switch(myButton){
		case Buttons.AboutButton:
			mAboutObject.SetActive(true);
			mMainMenuObject.SetActive(false);
			break;
		case Buttons.NewGameButton:
			Application.LoadLevel("LevelLoader");
			break;
		case Buttons.LoadButton:
			mLoadMenuObject.SetActive(true);
			mMainMenuObject.SetActive(false);
			break;
		}
	}
	void OnMouseOver(){
		gameObject.GetComponent<SpriteRenderer> ().sprite = mButtonOver;
	}
	void OnMouseExit(){
		gameObject.GetComponent<SpriteRenderer> ().sprite = mButton;
	}
	*/
}
